import { Router } from 'express';
// import { createPuzzle, getPuzzles, getPuzzle, updatePuzzle, deletePuzzle } from '../controllers/puzzleController';

const router = Router();

// @route   POST /api/puzzles
// @desc    Create a new puzzle
// router.post('/', createPuzzle);

// @route   GET /api/puzzles
// @desc    Get all puzzles
// router.get('/', getPuzzles);

// @route   GET /api/puzzles/:id
// @desc    Get a single puzzle
// router.get('/:id', getPuzzle);

// @route   PUT /api/puzzles/:id
// @desc    Update a puzzle
// router.put('/:id', updatePuzzle);

// @route   DELETE /api/puzzles/:id
// @desc    Delete a puzzle
// router.delete('/:id', deletePuzzle);

export default router;
